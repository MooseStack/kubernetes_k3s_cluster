[defaults]
inventory       = ./1-install-k3s-with-ansible/inventory.yaml

## Force downloads of ansible-galaxy installs to save in this git repo
collections_path = ./1-install-k3s-with-ansible/collections

become_method   = sudo
sudo_user       = ansible
become_ask_pass = False
ask_pass        = False